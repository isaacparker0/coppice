load("//tools/bazel/macros:rust.bzl", "rust_library")

rust_library(
    name = "diagnostics",
    srcs = [
        "diagnostic.rs",
        "lib.rs",
    ],
    visibility = ["//:__subpackages__"],
    deps = ["//compiler/source"],
)
