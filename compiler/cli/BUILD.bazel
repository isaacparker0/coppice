load("//tools/bazel/macros:rust.bzl", "rust_binary")

rust_binary(
    name = "main",
    srcs = ["main.rs"],
    visibility = ["//visibility:public"],
    deps = [
        "//compiler/driver",
        "//compiler/reports",
        "@crates//:clap",
        "@crates//:serde",
        "@crates//:serde_json",
    ],
)
