[default]
extend-ignore-re = [
  # Rust syn AST enum variant name.
  "Expr::Paren",
]

[default.extend-words]
# Commonly used abbreviations, banned per styleguide.
alnum = "alphanumeric"
paren = "parenthesis"
prev = "previous"
ty = "type"
