function update() -> int64 {
    mut x := 1
    x = 2
    return x
}

function nested() -> int64 {
    mut x := 1
    if true {
        x = 2
        mut x := 3
        x = 4
        return x
    }
    return x
}
