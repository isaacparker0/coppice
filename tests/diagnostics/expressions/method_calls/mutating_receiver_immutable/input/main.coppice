type User :: struct {
    id: int64,
    function bump(mut self, delta: int64) -> int64 {
        self = User { id: self.id + delta }
        return self.id
    },
}

function main() -> int64 {
    user := User { id: 1 }
    return user.bump(1)
}
