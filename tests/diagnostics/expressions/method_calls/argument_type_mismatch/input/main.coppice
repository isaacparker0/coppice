type User :: struct {
    id: int64,
    function plus(self, delta: int64) -> int64 {
        return self.id + delta
    },
}

function main() -> int64 {
    user := User { id: 1 }
    return user.plus(true)
}
