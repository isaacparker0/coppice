load("//tools/bazel/macros:rust.bzl", "rust_test")

rust_test(
    name = "diagnostics_test",
    srcs = ["diagnostics_test.rs"],
    data = [
        "//compiler/cli:main",
    ] + glob(["**"]),
    deps = [
        "//compiler/reports",
        "@crates//:serde_json",
        "@rules_rust//tools/runfiles",
    ],
)
