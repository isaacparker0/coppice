import workspace/auth { makeToken }
import workspace/auth { Token as AuthToken }

function idOf(token: AuthToken) -> int64 {
    return token.id()
}

function run() -> string {
    return idOf(makeToken())
}
