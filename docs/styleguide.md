# Styleguide

- Prefer precise, unambiguous names; longer is fine when it adds clarity, but avoid length for its own sake.
- Use full words instead of abbreviations.
- Avoid redundant names/comments; if the name is clear, drop the comment.
